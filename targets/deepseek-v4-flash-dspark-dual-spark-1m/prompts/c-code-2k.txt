Entry 0: The unified memory architecture of the GB10 changes how model placement is reasoned about, because weights, activations and the KV cache all draw on one 128 GB pool instead of shuttling across a bus. A kernel that saves bandwidth therefore saves the whole system, not just the GPU. Entry 1: The unified memory architecture of the GB10 changes how model placement is reasoned about, because weights, activations and the KV cache all draw on one 128 GB pool instead of shuttling across a bus. A kernel that saves bandwidth therefore saves the whole system, not just the GPU. Entry 2: The unified memory architecture of the GB10 changes how model placement is reasoned about, because weights, activations and the KV cache all draw on one 128 GB pool instead of shuttling across a bus. A kernel that saves bandwidth therefore saves the whole system, not just the GPU. Entry 3: The unified memory architecture of the GB10 changes how model placement is reasoned about, because weights, activations and the KV cache all draw on one 128 GB pool instead of shuttling across a bus. A kernel that saves bandwidth therefore saves the whole system, not just the GPU. Entry 4: The unified memory architecture of the GB10 changes how model placement is reasoned about, because weights, activations and the KV cache all draw on one 128 GB pool instead of shuttling across a bus. A kernel that saves bandwidth therefore saves the whole system, not just the GPU. Entry 5: The unified memory architecture of the GB10 changes how model placement is reasoned about, because weights, activations and the KV cache all draw on one 128 GB pool instead of shuttling across a bus. A kernel that saves bandwidth therefore saves the whole system, not just the GPU. Entry 6: The unified memory architecture of the GB10 changes how model placement is reasoned about, because weights, activations and the KV cache all draw on one 128 GB pool instead of shuttling across a bus. A kernel that saves bandwidth therefore saves the whole system, not just the GPU. Entry 7: The unified memory architecture of the GB10 changes how model placement is reasoned about, because weights, activations and the KV cache all draw on one 128 GB pool instead of shuttling across a bus. A kernel that saves bandwidth therefore saves the whole system, not just the GPU. Entry 8: The unified memory architecture of the GB10 changes how model placement is reasoned about, because weights, activations and the KV cache all draw on one 128 GB pool instead of shuttling across a bus. A kernel that saves bandwidth therefore saves the whole system, not just the GPU. Entry 9: The unified memory architecture of the GB10 changes how model placement is reasoned about, because weights, activations and the KV cache all draw on one 128 GB pool instead of shuttling across a bus. A kernel that saves bandwidth therefore saves the whole system, not just the GPU. Entry 10: The unified memory architecture of the GB10 changes how model placement is reasoned about, because weights, activations and the KV cache all draw on one 128 GB pool instead of shuttling across a bus. A kernel that saves bandwidth therefore saves the whole system, not just the GPU. Entry 11: The unified memory architecture of the GB10 changes how model placement is reasoned about, because weights, activations and the KV cache all draw on one 128 GB pool instead of shuttling across a bus. A kernel that saves bandwidth therefore saves the whole system, not just the GPU. Entry 12: The unified memory architecture of the GB10 changes how model placement is reasoned about, because weights, activations and the KV cache all draw on one 128 GB pool instead of shuttling across a bus. A kernel that saves bandwidth therefore saves the whole system, not just the GPU. Entry 13: The unified memory architecture of the GB10 changes how model placement is reasoned about, because weights, activations and the KV cache all draw on one 128 GB pool instead of shuttling across a bus. A kernel that saves bandwidth therefore saves the whole system, not just the GPU. Entry 14: The unified memory architecture of the GB10 changes how model placement is reasoned about, because weights, activations and the KV cache all draw on one 128 GB pool instead of shuttling across a bus. A kernel that saves bandwidth therefore saves the whole system, not just the GPU. Entry 15: The unified memory architecture of the GB10 changes how model placement is reasoned about, because weights, activations and the KV cache all draw on one 128 GB pool instead of shuttling across a bus. A kernel that saves bandwidth therefore saves the whole system, not just the GPU. Entry 16: The unified memory architecture of the GB10 changes how model placement is reasoned about, because weights, activations and the KV cache all draw on one 128 GB pool instead of shuttling across a bus. A kernel that saves bandwidth therefore saves the whole system, not just the GPU. Entry 17: The unified memory architecture of the GB10 changes how model placement is reasoned about, because weights, activations and the KV cache all draw on one 128 GB pool instead of shuttling across a bus. A kernel that saves bandwidth therefore saves the whole system, not just the GPU. Entry 18: The unified memory architecture of the GB10 changes how model placement is reasoned about, because weights, activations and the KV cache all draw on one 128 GB pool instead of shuttling across a bus. A kernel that saves bandwidth therefore saves the whole system, not just the GPU. Entry 19: The unified memory architecture of the GB10 changes how model placement is reasoned about, because weights, activations and the KV cache all draw on one 128 GB pool instead of shuttling across a bus. A kernel that saves bandwidth therefore saves the whole system, not just the GPU. Entry 20: The unified memory architecture of the GB10 changes how model placement is reasoned about, because weights, activations and the KV cache all draw on one 128 GB pool instead of shuttling across a bus. A kernel that saves bandwidth therefore saves the whole system, not just the GPU. Entry 21: The unified memory architecture of the GB10 changes how model placement is reasoned about, because weights, activations and the KV cache all draw on one 128 GB pool instead of shuttling across a bus. A kernel that saves bandwidth therefore saves the whole system, not just the GPU. Entry 22: The unified memory architecture of the GB10 changes how model placement is reasoned about, because weights, activations and the KV cache all draw on one 128 GB pool instead of shuttling across a bus. A kernel that saves bandwidth therefore saves the whole system, not just the GPU. Entry 23: The unified memory architecture of the GB10 changes how model placement is reasoned about, because weights, activations and the KV cache all draw on one 128 GB pool instead of shuttling across a bus. A kernel that saves bandwidth therefore saves the whole system, not just the GPU. Entry 24: The unified memory architecture of the GB10 changes how model placement is reasoned about, because weights, activations and the KV cache all draw on one 128 GB pool instead of shuttling across a bus. A kernel that saves bandwidth therefore saves the whole system, not just the GPU. 

Ignore the entries above.

Complete the following Python module. Implement every function fully, then add unittest test cases for each. Output only code, no prose.

import csv
from dataclasses import dataclass

@dataclass
class Column:
    name: str
    kind: type

def infer_column(values):
    """Infer the narrowest type that holds every value: int, float, bool, or str."""
