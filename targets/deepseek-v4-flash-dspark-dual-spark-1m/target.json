{
  "slug": "deepseek-v4-flash-dspark-dual-spark-1m",
  "name": "DeepSeek V4 Flash DSpark \u2014 dual Spark, 1M context",
  "recipe": "deepseek-v4-flash-dspark-dual-spark-1m",
  "recipeUrl": "https://howtospark.com/recipes/deepseek-v4-flash-dspark-dual-spark-1m",
  "status": "blocked",
  "statusNote": "Serves, decodes at 26.6 tok/s, and greedy output is provably identical across boots -- but it cannot hold a 4-boot baseline. Roughly half of all long TP2 runs deadlock inside a collective. That is the only thing left, and it is upstream of the arena.",
  "blockers": [
    {
      "id": "multi-node-determinism",
      "severity": "hard",
      "resolved": "2026-07-29",
      "what": "Whether greedy token identity survives an NCCL allreduce was unmeasured, and no TP target could be ranked until it was settled.",
      "answer": "It survives. Two boots, TP2 across two Sparks, NCCL over RoCE, warmup discarded: 4/4 prompts byte-identical (see results/_multinode/determinism-2026-07-29.txt). Neither boot hung, which also supports the standing theory that the 07-28 hangs were the TCP-socket fallback rather than a kernel fault.",
      "sharedWith": "glm-5-2-dual-spark-tp2 and every other dual-Spark recipe \u2014 this is a property of the collective, not of a model.",
      "scope": "Two boots at 8192 context, one config, four prompts. Enough to make multi-node rankable; the baseline's per-prompt stability screening still applies.",
      "nextStep": "Record the arena baseline."
    },
    {
      "id": "memlock-8mb",
      "severity": "soft",
      "resolved": "2026-07-29",
      "what": "ulimit -l was 8 MB on both nodes, so NCCL could not use RDMA and fell back to TCP sockets. A paired ratio measured over sockets is not the recipe's hardware.",
      "howFixed": "limits.d memlock unlimited on both nodes, verified as unlimited in the launch path itself rather than in a shell.",
      "trap": "The fix READ as failed for a day. Tailscale SSH terminates inside tailscaled and never runs pam_limits, so any ulimit taken over the tailnet reports systemd's 8 MB default no matter what limits.d says. Ray workers inherit whatever launched them, so a cluster started from a tailnet session gets 8 MB memlock on every raylet and NCCL silently falls back to sockets EVEN WITH THE ROOT FIX APPLIED. Launch the head via `ssh localhost` and the worker over its LAN address, and assert unlimited on both inside the script."
    },
    {
      "id": "tp2-collective-deadlock",
      "severity": "hard",
      "opened": "2026-07-29",
      "what": "About half of all long TP2 runs deadlock. Both ranks spin at 100% CPU while the GPUs draw ~14 W and one reports 0% utilisation -- a busy-wait inside a collective that never completes. It ends as `TimeoutError: RPC call to sample_tokens timed out` on whatever prompt happened to be in flight.",
      "notTheCause": [
        "NOT the interconnect. Every run today was verifiably on RoCE (NET/IB, 200 Gb/s virtual devices), which retires the earlier TCP-socket-fallback theory from 07-28.",
        "NOT kernel JIT. The 37-minute hang logged ZERO new JIT events; the six in that boot all landed early and the run was fine after them.",
        "NOT a prompt or a shape. b-prose-short is 64 tokens and decoded at 26.546 tok/s an hour earlier, then hung on a later boot under an identical config.",
        "NOT memory. kv_cache_usage was 0.5% and the oom-guard never tripped."
      ],
      "evidence": "4 long runs on 2026-07-29: two baseline arms died on c-code-2k (at 10 and 167 output tokens), one full-length warm pass hung on b-prose-short for 37 min, and one 16-token warm pass completed all four shapes. Two full boots on the morning determinism probe also completed cleanly. So it is intermittent at roughly 50% per long run -- which makes P(4 clean boots) too low to record a baseline.",
      "whyItBlocks": "A baseline needs 4 boots to agree. At this failure rate it will not finish, and a partial baseline is worse than none: it would define goldens from whichever prompts happened to survive.",
      "nextStep": "Bisect the deadlock outside the arena: try --enforce-eager (removes cudagraph capture from the collective path), then --distributed-executor-backend mp instead of ray, then NCCL_ASYNC_ERROR_HANDLING/NCCL_BLOCKING_WAIT to convert the busy-wait into a diagnosable abort. Capture py-spy dumps of both ranks mid-hang -- that names the stalled collective, which nothing measured so far does."
    },
    {
      "id": "oom-guard-headroom",
      "severity": "soft",
      "what": "oom-guard.sh trips at MemAvailable < 8192 MiB and killed a run that missed by 321 MiB, silently. The 6 GiB KV pin clears it, but the margin is thin and an arena arm must not die mid-measurement.",
      "nextStep": "Either raise the guard for arena runs or keep the KV pin at 6 GiB and verify headroom before each arm."
    }
  ],
  "why": "Worth scaffolding despite being blocked: at 1,048,576 tokens of context across two nodes with FP4 experts and FP8 dense, it exercises paths nothing else in the corpus does \u2014 and it is the recipe most likely to reward kernel work, because almost nothing on that path was written for sm_121.",
  "served": {
    "on": "2026-07-28",
    "engine": "vLLM 0.26.0 (stock wheel + arena kernel overlay)",
    "nodes": 2,
    "blockSize": 256,
    "kvCacheTokens": 17009,
    "maxConcurrency": "2.08x at 8192/request",
    "healthyAfterSeconds": 220,
    "firstLightDecodeTps": 16.2,
    "note": "First light, not a benchmark: --max-model-len 8192, NCCL over sockets because memlock is 8 MB, and there is no page-size-64 baseline to compare against because the model cannot run at 64 at all.",
    "serveArgs": [
      "--distributed-executor-backend",
      "ray",
      "--tensor-parallel-size",
      "2",
      "--block-size",
      "256",
      "--kv-cache-dtype",
      "fp8",
      "--max-model-len",
      "8192",
      "--gpu-memory-utilization",
      "0.70",
      "--kv-cache-memory-bytes",
      "6442450944",
      "--max-num-seqs",
      "1",
      "--max-num-batched-tokens",
      "8192",
      "--trust-remote-code"
    ],
    "env": {
      "NCCL_IB_DISABLE": "1",
      "RAY_local_fs_capacity_threshold": "1",
      "RAY_memory_monitor_refresh_ms": "0",
      "PYTHONPATH": "<arena>/patches/flashinfer-dsv4-pbs256"
    }
  },
  "model": "~/models/hf/DeepSeek-V4-Flash",
  "servedModelName": "dsv4",
  "port": 8000,
  "seed": 0,
  "defaultMaxTokens": 256,
  "promptSettings": {
    "d-doc-8k": { "maxTokens": 128 }
  },
  "promptSettingsNote": "d-doc-8k is ~7.7k tokens and --max-model-len is 8192, so its output budget has to be small enough to fit. A request that overruns the context aborts the arm.",
  "startupTimeout": 1800,
  "cacheRoot": "~/.cache/vllm-dsv4",
  "patchRequired": "flashinfer-dsv4-pbs256",
  "patchNote": "This target does not run at all without the overlay: the stock DSv4 decode kernel is instantiated only at PAGE_BLOCK_SIZE 64, and the model needs 256. The overlay is therefore part of the BASELINE here, not a candidate -- both arms carry it.",
  "cluster": {
    "nodes": 2,
    "nic": "enp1s0f1np1",
    "requireRdma": true,
    "head": { "ip": "192.168.100.1" },
    "workers": [{ "host": "192.168.0.28", "ip": "192.168.100.2" }],
    "hostNote": "workers[].host is a LAN address reached over real OpenSSH. It must NOT be a tailnet address: tailscaled terminates the session itself and never runs pam_limits, so every raylet would inherit an 8 MB memlock and NCCL would fall back to TCP sockets without saying so.",
    "preflight": [
      {
        "name": "sparse-mla-sm120",
        "python": "from vllm.utils.flashinfer import has_flashinfer_sparse_mla_sm120 as f; print(f())",
        "expect": "True",
        "hint": "Almost always nvcc missing from PATH -- vLLM's has_flashinfer() is False without it because FlashInfer JITs, and the error then names the sparse MLA API, which is present and working. Check /usr/local/cuda/bin is on PATH and that the venv's bin (ninja) is still on it."
      }
    ]
  },
  "serveArgs": [
    "--served-model-name", "dsv4",
    "--distributed-executor-backend", "ray",
    "--tensor-parallel-size", "2",
    "--block-size", "256",
    "--kv-cache-dtype", "fp8",
    "--max-model-len", "8192",
    "--gpu-memory-utilization", "0.70",
    "--kv-cache-memory-bytes", "6442450944",
    "--max-num-seqs", "1",
    "--max-num-batched-tokens", "8192",
    "--trust-remote-code"
  ],
  "serveArgsNote": "--max-num-seqs 1 is mandatory: batching breaks token identity on this part (n=2 -> 2 distinct outputs), and the whole cheap gate rests on greedy determinism. --max-model-len is 8192, NOT the recipe's 1M -- the KV pin that clears the oom-guard does not leave room for it, and a target that cannot finish an arm cannot rank anything. A 1M-context variant is its own target.",
  "env": {
    "RAY_local_fs_capacity_threshold": "1",
    "RAY_memory_monitor_refresh_ms": "0",
    "VLLM_EXECUTE_MODEL_TIMEOUT_SECONDS": "900",
    "NCCL_DEBUG": "INFO",
    "NCCL_DEBUG_SUBSYS": "INIT,NET"
  },
  "weights": {
    "hfRepo": "deepseek-ai/DeepSeek-V4-Flash",
    "public": true,
    "sizeBytes": 238370000000,
    "note": "222 GB on disk. Public and ungated, so it satisfies policy.publicWeights -- but it is the largest entry cost in either arena, and it must be present on BOTH nodes."
  }
}
